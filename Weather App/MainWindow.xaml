<Window x:Class="Weather_App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:vm="clr-namespace:Weather_App.ViewModels"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Weather_App"
        mc:Ignorable="d"
        Title="Weather App" Height="450" Width="380"
        ResizeMode="NoResize"
        >
    <Window.Background>
        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#51668F" Offset="0.0" />
            <GradientStop Color="#10E1B2" Offset="1.0" />
        </LinearGradientBrush>
    </Window.Background>
    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!--Search-->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Grid.ColumnSpan="4" Grid.Row="0" >
            <TextBox  Grid.Row="0" Grid.ColumnSpan="3" x:Name="searchCityTextBox" Text="{Binding SearchQuery, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            <Button Grid.Row="4" Grid.Column="4" x:Name="aplyBtn" Command="{Binding SearchCommand}">
                <Image Source="pack://application:,,,/Assets/search.png" Stretch="Uniform"/>
            </Button>
        </StackPanel>
        <!--Main temp-->
        <StackPanel Margin="0 -30 0 10" Grid.Column="1" Grid.ColumnSpan="2" Grid.Row="1" Grid.RowSpan="2">
            <Image x:Name="MainTemperatureImg" Grid.Row="1" Style="{StaticResource MainWeatherImg}" Source="pack://application:,,,/Assets/clear.png"/>
            <TextBlock Style="{StaticResource MainCity}" Text="{Binding CityName}"/>
            <TextBlock Style="{StaticResource InfoLbl}" Text="{Binding CountryName}"/>
            <TextBlock Style="{StaticResource MainInfo}" Text="{Binding Temperature}"/>
        </StackPanel>
            <!-- Hum Info-->
        <WrapPanel Margin="15" Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="4">
            <Image Style="{StaticResource AddedImg}" Source="pack://application:,,,/Assets/humidity.png"/>
            <StackPanel>
                <TextBlock Style="{StaticResource ExtraInfo}" Text="{Binding Humidity}"/>
                <Label Content="humidity"/>
            </StackPanel>
        </WrapPanel>
        
        <!--Wind Info-->
        <WrapPanel Margin="30 15 15 15" Grid.Column="2" Grid.ColumnSpan="2" Grid.Row="4">
            <Image Style="{StaticResource AddedImg}" Source="pack://application:,,,/Assets/wind.png"/>
            <StackPanel>
                <TextBlock Style="{StaticResource ExtraInfo}" Text="{Binding WindSpeed}"/>
                <Label Content="wind speed"/>
            </StackPanel>
        </WrapPanel>
    </Grid>
</Window>
