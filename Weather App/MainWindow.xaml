<Window x:Class="Weather_App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:vm="clr-namespace:Weather_App.ViewModels"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Weather_App"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Grid.Column="0" x:Name="cityName" FontSize="50" Text="{Binding CityName}"/>
        <TextBlock Grid.Row="0" Grid.Column="1" x:Name="countryName" FontSize="50" Text="{Binding CountryName}"/>
        <TextBlock Grid.Row="0" Grid.Column="2" x:Name="tempValue" FontSize="50" Text="{Binding Temperature}"/>
        <TextBlock Grid.Row="0" Grid.Column="3" x:Name="humanidyValue" FontSize="50" Text="{Binding Humidity}"/>
        <TextBlock Grid.Row="0" Grid.Column="4" x:Name="windValue" FontSize="50" Text="{Binding WindSpeed}"/>
        <TextBox Grid.Row="1" Grid.Column="2" x:Name="searchCityTextBox" Text="{Binding SearchQuery, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
        <Button Grid.Row="1" Grid.Column="3" x:Name="aplyBtn" Content="Search" Command="{Binding SearchCommand}"/>
    </Grid>
</Window>
